# AI Werewolf Arena · 月下议事厅

一个免费、本地运行的多智能体游戏主持台，内置「经典狼人杀」与独立游戏「爆炸项圈」。它不调用任何 AI API：主持人把程序生成的中文 Prompt 复制到不同 AI 网页，再将回复粘贴回来。程序负责信息隔离、状态推进、行动解析、规则结算、胜负判断和本地存档。

> 模型厂商名称只作为用户自行填写的标签。相关商标归各自所有者所有。本项目与任何模型厂商均无官方关系。

## 功能

- 两个彼此隔离、均可完整游玩的游戏模式：经典 7 人狼人杀与 4–8 人爆炸项圈。
- 经典 7 人局：2 狼人、预言家、女巫、猎人、2 村民；支持随机或手动分配。
- 爆炸项圈包含随机线路、私人安全提示、他人扫描、剪线保险、轮换操作者和最后幸存者结算。
- 完整上帝控制状态机，AI 输出不能自行推进阶段。
- 狼人一致意见、预言家查验、女巫双药限制、猎人开枪条件、平票 PK 与阵营胜负结算。
- 公共信息、玩家私人信息、回复和上帝日志逻辑隔离。
- Prompt 模板按通用规则和角色拆分，包含提示注入防护边界。
- 回复先解析预览，再由上帝确认；解析失败可手动指定行动。
- 深色桌游控制台提供阶段轨迹、行动进度、待行动玩家快捷定位、一键隐私遮罩与响应式布局。
- 行动工作台按“提示词—AI 回复—确认”引导操作，并支持生成后自动复制提示词。
- 公共、私人和上帝日志独立显示，支持数量提示、玩家切换与当前日志搜索。
- 主持快捷栏可快速跳转本轮、席位、行动与记录，减少长对局中的反复滚屏。
- 两种模式共用本地房间列表、JSON 原子存档、恢复备份、撤销、导入导出和二次确认删除。
- 完整存档支持导出和复制导入，不会覆盖已有对局；旧版存档可自动迁移。
- 进行中可导出不泄密的 Markdown 公开战报；结束后可导出 Markdown 完整复盘。
- 刷新浏览器后从服务端存档恢复，不使用数据库、遥测或外部网络请求。

## 环境与安装

需要 Node.js 20+ 和 npm（Windows、macOS、Linux 均可）。

```bash
npm install
npm run dev
```

打开终端显示的地址（前端通常为 `http://127.0.0.1:5173`）。开发模式同时启动 Vite 前端和 Express 服务。

生产构建与运行：

```bash
npm run build
npm start
```

然后打开 `http://127.0.0.1:3001`。

## 常用命令

```bash
npm run dev       # 开发模式
npm test          # 单元测试
npm run typecheck # 前后端类型检查
npm run lint      # ESLint
npm run format    # Prettier 格式化
npm run build     # 生产构建
npm run test:e2e  # Playwright 浏览器流程测试
```

首次运行浏览器测试前执行 `npx playwright install chromium` 安装测试专用浏览器。

## 经典狼人杀玩法

1. 新建对局，填写玩家名称和用于区分网页的“模型标签”，选择随机或手动身份。
2. 在身份确认页由上帝核对座位，然后正式开始。
3. 对当前等待行动的玩家生成 Prompt 并复制到对应 AI 网页。
4. 把回复粘贴回行动工作台，点击解析，检查预览后确认提交。严格解析失败时可主动使用“宽松重试”，仍无法解析时再用手动模式。
5. 所有必要行动收齐后，由上帝点击“推进至下一阶段”。狼人意见不一致时选择裁定目标或空刀。
6. 对局自动保存。公开战报可随时导出；完整复盘只在游戏结束后开放。完整存档备份可在大厅重新导入，并作为新对局保存。

## 狼人杀默认规则

首夜是否允许女巫自救可配置，此后不可自救；同夜不能使用两瓶药。猎人被狼刀或放逐时可开枪，被毒死不能开枪。首轮平票进入 PK，PK 再平票无人出局。死亡身份是否公开可配置，默认不公开。狼人全灭则好人获胜；存活狼人数达到存活好人数则狼人获胜。夜间死亡是否有遗言可在建局时配置。

## 爆炸项圈玩法

爆炸项圈是独立的生存、情报与虚张声势游戏，支持 4–8 名玩家，默认 5 人：

1. 每名玩家的项圈有红、蓝、黄三根线，其中一根是致命线。
2. 每名玩家只知道自己的一根安全线，以及另一名玩家的一根安全线扫描结果；这些信息均为私人信息。
3. 主持人在开局页逐一生成并发送私人简报，确认信息隔离后正式开始。
4. 所有人完成开场陈述。之后按存活座位轮换操作者。
5. 每轮由当前操作者发言，然后选择另一名存活玩家和一根尚未剪断的线路。
6. 目标看到剪线声明后，可以消耗全局唯一一次个人保险取消本次剪线，或接受剪线。
7. 安全线被剪断后公开记录；致命线被剪断时目标立即淘汰。已剪断线路不能再次选择。
8. 结算公开后进入下一轮；最后一名存活玩家获胜。

爆炸项圈支持的严格回复格式：

```text
【公开发言】你的发言
【剪线：3号-红】
【使用保险】
【接受剪线】
```

每次解析仍只生成候选结果，必须由主持人确认后才写入状态。解析失败时可手动选择目标、线色或应对方式。

## 隐私模型

服务端保留完整状态，前端上帝控制台可以按开关查看身份。每次生成玩家 Prompt 都由服务端按该玩家身份和当前阶段重新组装：狼人只获得狼队友，预言家只获得自己的查验记录，女巫只获得允许看到的当夜信息和自身药物状态。公共发言被包在不可信游戏内容边界内，不作为系统规则。

爆炸项圈使用独立的私人信息模型：玩家 Prompt 只包含本人的已知安全线、保险状态和自己持有的一条扫描结果。进行中公开状态只包含存活情况、已剪断线路、保险是否可用和公共发言；致命线、其他玩家的安全提示、私人扫描和完整主持状态不会进入玩家 Prompt 或公开战报。完整复盘仅在对局结束后开放。

本项目的“隐私”是本地主持流程中的逻辑隔离：上帝拥有机器和存档的完全访问权。不要把整个控制台展示给参赛玩家。运行数据位于 `data/games/`，已加入 `.gitignore`。完整存档备份包含身份、私人行动和原始回复，应按私人文件保管；需要公开分享时只使用公开战报。

## 目录结构

```text
server/             Express API 与 JSON 存档
src/game/           两种模式的纯规则引擎、解析器、存档校验与战报
src/prompts/        狼人杀角色模板与爆炸项圈 Prompt 模板
src/shared/         前后端共享类型
src/App.tsx         React 上帝控制台
src/components/     通用控制台、爆炸项圈控制台与日志组件
tests/              规则、隐私、解析和存档测试
data/games/         本地运行数据（不会提交）
```

## 已知限制

- 狼人杀随机身份分配固定为 7 人经典配置；手动模式可创建至少 5 人对局，但规则平衡由主持人负责。
- 爆炸项圈当前固定使用三线、一条自身安全提示、一条他人安全扫描和可配置的一次保险，不提供自定义线路数量或道具牌组。
- 采用半自动复制/粘贴，不控制第三方网页，也不实现浏览器自动化或 API adapter。
- 一台服务实例适合一名本地上帝，不含登录、多用户并发和存档加密。
- 当前存档迁移支持无版本号 MVP 存档和 schema v1；更高版本会被安全拒绝，不会强行降级覆盖。
- 自然语言解析有意保持保守：只有规定标记会生成候选行动，最终仍需人工确认。
- 游戏规则采用项目内置版本，暂不支持自定义角色或复杂房规。

## 截图

截图占位：可将模式大厅、狼人杀控制台和爆炸项圈剪线阶段截图放入 `docs/screenshots/`，再在此处补充图片链接。仓库当前不包含真实对局截图，避免意外公开私人数据。

## 安全说明

当前版本不需要 `.env` 或密钥。未来 adapter 的密钥只能保存在本地 `.env`，不得提交 Cookie、会话数据、真实私人对局或任何凭据。默认服务仅监听 `127.0.0.1`。

## 内容与责任说明

本项目是本地运行的娱乐及技术实验工具，不隶属于任何模型厂商，也不代表任何厂商立场。游戏中由模型生成的发言不代表项目作者的观点。

用户应自行确保输入、模型输出、对局记录及使用方式符合所在地法律、模型平台规则与基本社区规范。请勿使用本项目生成、保存或传播违法、骚扰、仇恨、侵犯隐私或其他有害内容；默认不要公开真实对局记录或包含个人信息的内容。

## License

[MIT](LICENSE)
